{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "dduo-solo-founder": {
      "type": "stdio",
      "command": "node",
      "args": [
        "${PLUGIN_ROOT}/bin/agent-plugin-mcp.mjs"
      ],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
